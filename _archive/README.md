# `_archive/` — history only

Superseded docs, Cursor rules, one-off scripts, result files, old audits and the Orbit With Ben leftovers. Archived 25 Sep 2026 in the studio clean-up (`00_Brand/Channel-Setup/audits/HOS_STUDIO_AUDIT_2026-09-25/AUDIT.md`).

- **Don't follow anything here** unless Ben asks for it. The rules in force are listed in `AGENTS.md` at the repo root.
- Paths are preserved: `_archive/<old path>`. Nothing was deleted, only `git mv`'d.
- `cursor-rules/` holds the 34 old `.cursor/rules/*.mdc` files as `.md`, so Cursor no longer loads them.
- `_archive_orbit/` is the Orbit With Ben template leftovers (now in its own repo, `orbit-with-ben`). Never ship them to `@HistoryOfScienceYT`.
- `.cursorignore` keeps this folder out of agent context.

Every house rule from the archived HOS locks (UAT bible, plate library, VO and teach, teaching density, silent-readable picture, no DNA helix, open/out, Explorer, microbes) is carried into `00_Brand/Channel-Setup/STUDIO_PLAYBOOK.md`.

# HOS 002 Part 03 — v06 props-only remint (Atomic Weights)

**Gate:** Ben OVERRIDE 7 Sep 2026 — blank papers under `ATOMIC WEIGHTS` get sparse readable marks.  
**Parent:** `hos_002_part03_rough_v05.mp4` (sha256 `3642d005…`) — UAT PASS / Explorer on-model.  
**Export:** `hos_002_part03_rough_v06.mp4`  
**Account:** Flow `benoats@googlemail.com`. If Create dies → STOP (no Ken Burns).  
**Scores → CoS only.** CoS owns Ben ping after UAT. No P04 until Ben KEEP.

---

## Scope (props only)

| Remint | KEEP locked |
|--------|-------------|
| Paper marks on Atomic Weights beat only | Hall DNA (honey oak, arched windows, benches) |
| | Motion / real Veo (no Ken Burns) |
| | `05_explorer_ruler` teal trenchcoat |
| | `09_empty_chair_claim` vacant aisle chair |
| | All other plates 01, 02, 04–12 unless listed below |
| | Animistry side label **ATOMIC WEIGHTS** (16.0–26.0) — do not redesign |
| | VO `part03_ruler_for_atoms_v01` · bed/FX stack |

**Do not remint** Explorer, empty chair, hall architecture, mass-line glow language, or any plate outside the Atomic Weights props list.

---

## VO (this beat)

> Stanislao Cannizzaro wins with a method, not poetry. Treat molecules honestly. Derive atomic weights you can repeat. Put the same number on the same element everywhere.

Label window: **ATOMIC WEIGHTS** · 16.0–26.0s  
Overlaps mainly **`03_method_pamphlet`** (≈15.3–23.3s); early **`04_zoo_gets_ruler`** only if cards still read fully blank under the label.

---

## Plates to remint

### 1) `03_method_pamphlet` — REQUIRED (Ben stills)

Two looks Ben flagged (same plate / same beat):

1. **Wide hall swirl** — cream pamphlets mid-air down the aisle.  
2. **Desk hero** — one upright cream sheet + tidy stack beside it.

**Props change only:**
- On **a few** sheets only (≈3–6 in the swirl; upright hero + 1–2 on the stack): sparse phone-readable marks — element symbol + whole number, e.g. `H 1` · `O 16` · `C 12` · `N 14` · `S 32`.
- Dark ink / charcoal on cream — high contrast, huge enough to read on a phone in the clear frames.
- **Not** equations. **Not** full tables. **Not** a wall of text. **Not** paragraphs.
- **Most** sheets stay **blank** so the swirl still reads as paper chaos, not a spreadsheet.

**KEEP:** same Karlsruhe hall DNA, camera move (wide → desk settle), continuous motion, no room swap, no blackboard/chandelier/fresco, no Orbit, no vessel fire, no faces in hero CU.

**HARD REJECT:** every sheet covered in text; tiny unreadable chicken-scratch; formula walls; photoreal; layout reset; Ken Burns-only; Explorer on this plate; empty-chair redesign.

### 2) `04_zoo_gets_ruler` — OPTIONAL props touch

Only if, under the remaining `ATOMIC WEIGHTS` label (≈23–26s), aligning cards are still 100% blank and feel like the same FAIL Ben called.

- Then: sparse `H 1` / `O 16` / `C 12` on **2–3** cards as they lock to the mass-line.  
- Most cards blank. Mass-line stays blank wood glow (no etched numbers on the ruler).  
- If Picture judges the fail is fully fixed by `03` alone → **skip 04**.

---

## Still refs (Ben / CoS)

- Flying blank papers + `ATOMIC WEIGHTS` (wide aisle)  
- Upright blank paper on desk + stack + `ATOMIC WEIGHTS`

---

## Assemble

- Parent keep set = v05 plate pack.  
- Swap only reminted props plate(s).  
- Same side-label cue sheet (ATOMIC WEIGHTS stays).  
- Export `09_Final-Export/hos_002_part03_rough_v06.mp4` + sha.  
- Hand UAT → CoS (not Ben direct).

---

## Done when

Child-obvious on a phone: during `ATOMIC WEIGHTS`, at least a few papers clearly show **element + number**; swirl still mostly blank; hall/Explorer/empty chair unchanged from v05.
